#include "catch/catch.hpp"
#include "camera.hpp"
using namespace copexp;

TEST_CASE("Camera testing", "[opencv]")
{
#if 0
    CameraBuilder cam_builder;
    cam_builder.open();

    if (!cam_builder.isOpen())
    {
    }

    CameraView &camera = cam_builder.get();
#endif
}
